# =============================================================================
# Atlas 数据库迁移配置
# 复制此文件为 .env 并填入实际值
# =============================================================================

# -----------------------------------------------------------------------------
# 本地开发环境
# -----------------------------------------------------------------------------
LOCAL_DATABASE_URL=postgres://postgres:password@localhost:5432/litho

# -----------------------------------------------------------------------------
# 生产环境
# -----------------------------------------------------------------------------
# 数据库连接 URL（如使用 SSH 隧道，host 应为 localhost）
PROD_DATABASE_URL=postgres://prod_user:prod_password@localhost:15432/litho

# SSH 隧道配置（可选，如不需要可删除以下配置）
PROD_SSH_HOST=bastion.example.com
PROD_SSH_PORT=22
PROD_SSH_USER=deploy
PROD_SSH_KEY=~/.ssh/id_rsa

# 生产数据库内网地址（通过 SSH 隧道访问）
PROD_DB_HOST=prod-db.internal
PROD_DB_PORT=5432

# 本地端口映射（SSH 隧道本地端口）
PROD_LOCAL_PORT=15432
